/* using System;

public class Volume {
	public static void Main(string[] args) {
		int rad = 6378;
		
		double vol = (4 / 3) * Math.PI * rad * rad * rad;

		Console.WriteLine($"The volume of earth in cubic kilometers is {Math.Round(vol, 2)} and cubic miles is {Math.Round(vol, 2) * 1.6}");
	}
} */